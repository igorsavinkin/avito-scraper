# Парсер Avito.ru v2.0

Продвинутый парсер для сбора детальных данных об объявлениях о сдаче квартир посуточно в Волгограде с сайта Avito.ru.

## 🎯 Возможности

### Двухэтапный парсинг
- **Этап 1:** Сбор ссылок на объявления со страницы каталога
- **Этап 2:** Детальный парсинг каждой страницы объявления

### Собираемые данные
- ✅ Название объявления
- ✅ Ссылка на страницу
- ✅ Стоимость
- ✅ Первые 3 медиа (фото/видео)
- ✅ Информация о квартире (параметры)
- ✅ Правила проживания
- ✅ Адрес
- ✅ Полное описание
- ✅ Имя владельца
- ✅ Ссылка на профиль владельца

### Технические возможности
- Автоматический запуск браузера в фоновом режиме
- Сохранение данных в SQLite с двумя таблицами
- Сохранение и загрузка куки для избежания блокировок
- Возможность прерывать и продолжать парсинг
- Защита от дублирования данных
- Детальное логирование процесса
- Структурированный код с использованием ООП

## Установка

### Вариант 1: Автоматическая установка (рекомендуется)

**Для Windows (PowerShell):**
```powershell
.\setup.ps1
```

**Для Windows (CMD):**
```cmd
setup.bat
```

### Вариант 2: Ручная установка

1. Убедитесь, что у вас установлен Python 3.7+

2. Создайте виртуальное окружение:
```bash
python -m venv venv
```

3. Активируйте виртуальное окружение:

**Windows (PowerShell):**
```powershell
.\venv\Scripts\Activate.ps1
```

**Windows (CMD):**
```cmd
venv\Scripts\activate.bat
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

4. Обновите pip:
```bash
python -m pip install --upgrade pip
```

5. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Структура проекта

```
avito-bot/
├── main.py                 # Основной файл с интерфейсом (v2.0)
├── scraper.py              # Класс для управления браузером Selenium
├── html_parser.py          # Класс для парсинга HTML (расширенный)
├── db.py                   # Класс для работы с SQLite (2 таблицы)
├── requirements.txt        # Зависимости проекта
├── setup.bat              # Скрипт установки для Windows (CMD)
├── setup.ps1              # Скрипт установки для Windows (PowerShell)
├── check_dependencies.py  # Проверка установленных зависимостей
├── fix_chromedriver.py    # Исправление проблем с ChromeDriver
├── .gitignore            # Исключения для Git
├── README.md             # Основная документация
├── QUICKSTART.md         # Быстрый старт за 3 минуты
├── INSTALL.md            # Подробная инструкция по установке
├── USAGE.md              # Руководство по использованию v2.0
├── TROUBLESHOOTING.md    # Устранение неполадок
├── CHANGELOG.md          # История изменений
├── UPGRADE_GUIDE.md      # Руководство по обновлению до v2.0
└── COMMANDS.md           # Полезные команды
```

## Использование

### Запуск программы

```bash
python main.py
```

### Меню программы

1. **Запустить полный парсинг** - сбор ссылок + детальный парсинг (рекомендуется)
2. **Только собрать ссылки** - быстрый сбор URL объявлений
3. **Только парсить детальные страницы** - обработка собранных ссылок
4. **Показать статистику** - текущее состояние базы данных
5. **Показать все данные** - полный просмотр всех объявлений
6. **Очистить базу данных** - удаление всех данных
7. **Выход** - завершение работы

### Рабочий процесс

**Вариант 1: Полный парсинг (автоматический)**
```
Выберите: 1
→ Собирает ссылки
→ Парсит каждую страницу
→ Сохраняет в БД
```

**Вариант 2: Поэтапный (рекомендуется для больших объемов)**
```
Шаг 1: Выберите 2 (собрать ссылки)
Шаг 2: Выберите 3 (парсить детально)
```

### Программное использование

```python
from main import AvitoBot

# Создание экземпляра бота
bot = AvitoBot(headless=True)

# Полный парсинг
bot.run()

# Или поэтапно:
# Этап 1: Сбор ссылок
links_count = bot._collect_apartment_links()

# Этап 2: Детальный парсинг
parsed_count = bot._parse_apartment_details()

# Просмотр статистики
bot._print_statistics()

# Просмотр данных
bot.show_all_data()

# Очистка базы
bot.clear_database()
```

📖 **Подробное руководство:** См. [USAGE.md](USAGE.md)

## База данных

Данные сохраняются в файл `avito_data.db` с двумя таблицами:

### Таблица `apartment_links` (промежуточная)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Уникальный идентификатор |
| url | TEXT | URL объявления (уникальный) |
| is_parsed | BOOLEAN | Флаг обработки (0/1) |
| created_at | TIMESTAMP | Дата добавления |

### Таблица `apartments` (основная)

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | Уникальный идентификатор |
| title | TEXT | Название объявления |
| url | TEXT | Ссылка на страницу (уникальная) |
| price | TEXT | Стоимость |
| media_url_1 | TEXT | Ссылка на первое медиа |
| media_url_2 | TEXT | Ссылка на второе медиа |
| media_url_3 | TEXT | Ссылка на третье медиа |
| about_apartment | TEXT | О квартире (параметры) |
| rules | TEXT | Правила проживания |
| address | TEXT | Адрес |
| description | TEXT | Описание |
| owner_name | TEXT | Имя владельца |
| owner_url | TEXT | Ссылка на профиль владельца |
| created_at | TIMESTAMP | Дата добавления |

## Файлы

- `avito_data.db` - база данных SQLite
- `avito_cookies.pkl` - сохраненные куки браузера

## Особенности

### Обработка блокировок

- При первом запуске сохраняются куки браузера
- При последующих запусках куки загружаются автоматически
- Это помогает избежать блокировок от Avito

### Настройки браузера

- Браузер запускается в фоновом режиме (headless)
- Отключены изображения для ускорения загрузки
- Установлен реалистичный User-Agent
- Настроены дополнительные опции для стабильности

### Парсинг данных

- Использует несколько селекторов для поиска элементов
- Обрабатывает различные варианты структуры страницы
- Извлекает заголовки, цены и URL фотографий
- Очищает данные от лишних символов

## Устранение неполадок

### ❌ Ошибка ChromeDriver (WinError 193)

**Быстрое решение:**
```bash
python fix_chromedriver.py
```

**Ручное решение:**
```bash
pip uninstall -y selenium webdriver-manager
pip install selenium==4.15.2 webdriver-manager==4.0.1
```

### 🔍 Полная диагностика

```bash
# Проверка зависимостей
python check_dependencies.py

# Исправление ChromeDriver
python fix_chromedriver.py
```

### 📚 Подробное руководство

См. [TROUBLESHOOTING.md](TROUBLESHOOTING.md) для детального описания всех проблем и решений:
- Ошибки ChromeDriver
- Проблемы с парсингом
- Блокировки сайтом
- Ошибки базы данных
- И многое другое

## Требования

- Python 3.7+
- Google Chrome
- Интернет-соединение

## Зависимости

**Обязательные:**
- selenium==4.15.2
- beautifulsoup4==4.12.2
- webdriver-manager==4.0.1

**Опциональные:**
- lxml>=4.9.0 (для более быстрого парсинга, если не установлен - используется встроенный html.parser)

## Лицензия

Проект создан в образовательных целях. Соблюдайте правила использования сайта Avito.ru.
